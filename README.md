# Google Sheets Summarizer

A Node.js application that automatically analyzes and summarizes financial data from Google Sheets using AI-powered insights. The application uses Google's Google Sheets API to process a given Google Sheet via sharable URL, generates intelligent summaries using requests with OpenAI's GPT models, and delivers the responses (in the form of a report) to a specified email address via Node Mailer.